# Cognito Test Configuration
# Copy this file to .env and fill in your actual values

# AWS Configuration
AWS_REGION=us-east-1

# Cognito User Pool Configuration
COGNITO_USER_POOL_ID=us-east-1_xxxxxxxxx
COGNITO_CLIENT_ID=xxxxxxxxxxxxxxxxxxxxxxxxxx

# Optional: Client Secret (if your Cognito app client has a secret)
# When set, SECRET_HASH will be automatically calculated using HMAC-SHA256
COGNITO_CLIENT_SECRET=your-client-secret-here

# Username for SECRET_HASH calculation
COGNITO_USERNAME=test2@leanmcp.com

# Test Tokens
# ID Token: Should be EXPIRED - used to test expired token detection
# The test will verify this token is expired, then use refresh token to get new tokens
COGNITO_ID_TOKEN=eyJraWQiOiJ...your-expired-id-token-here...

# Refresh Token: REQUIRED - used to obtain new valid tokens
# Get this from Cognito sign-in response (does not expire quickly)
COGNITO_REFRESH_TOKEN=eyJjdHkiOiJ...your-refresh-token-here...
