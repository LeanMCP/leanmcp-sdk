# GitHub OAuth App credentials
# Create at: https://github.com/settings/developers
# Set callback URL to: https://chatgpt.com/connector_platform_oauth_redirect
GITHUB_CLIENT_ID=your_github_client_id
GITHUB_CLIENT_SECRET=your_github_client_secret

# Session secret for OAuth server (generate a random 32+ char string)
# Used for signing state parameters and as fallback for JWT secrets
SESSION_SECRET=your_random_session_secret_here_32_chars_min

# JWT Signing Secret (HS256) - Optional, defaults to SESSION_SECRET
# Generate: openssl rand -base64 32
JWT_SIGNING_SECRET=your_jwt_signing_secret_32_chars_min

# JWT Encryption Secret (AES-256) - Optional, defaults to SESSION_SECRET
# Generate: openssl rand -hex 32
# Must be 32 bytes (64 hex characters) for AES-256
JWT_ENCRYPTION_SECRET=0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef

# Public URL of your server (required for OAuth)
# Use ngrok for local development: ngrok http 3300
# Example: https://abc123.ngrok-free.app
PUBLIC_URL=https://your-ngrok-url.ngrok-free.app

# OpenAI API key for roast generation
OPENAI_API_KEY=sk-...

# Server port
PORT=3300

# For local testing without OAuth, use a Personal Access Token (DEPRECATED - Use OAuth instead)
# Create at: https://github.com/settings/tokens
# Required scopes: read:user, repo (optional for private repos)
# GITHUB_TOKEN=ghp_...
